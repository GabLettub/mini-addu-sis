function displaySelected() { // gets the selected item
    var prelim = document.getElementById("prelim").value;
    document.getElementById("prelimSelectedGrade").innerText = "prelim grade " + prelim;
}